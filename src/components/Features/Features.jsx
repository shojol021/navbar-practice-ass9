import React from 'react';

const Features = ({ features }) => {
    console.log(features)
    return (
        <div>
            <h3>{features}</h3>
        </div>
    );
};

export default Features;